'use client'
import { useEffect, useState } from 'react'
import { supabase } from '../../lib/supabase'
import type { Listing } from '../../lib/types'

export default function Admin(){const [allowed,setAllowed]=useState<boolean|null>(null);const [pending,setPending]=useState<Listing[]>([]);const [message,setMessage]=useState('');async function load(){const {data:{user}}=await supabase.auth.getUser();if(!user){location.href='/auth';return}const {data:p}=await supabase.from('profiles').select('role').eq('id',user.id).single();if(p?.role!=='admin'){setAllowed(false);return}setAllowed(true);const {data}=await supabase.from('tutor_listings').select('*').eq('status','pending').order('created_at');setPending(data||[])}useEffect(()=>{load()},[]);async function review(id:string,status:'active'|'rejected'){const {error}=await supabase.from('tutor_listings').update({status}).eq('id',id);setMessage(error?error.message:`Listing ${status}.`);if(!error)load()}if(allowed===null)return <main className="dash">Checking admin permissions…</main>;if(!allowed)return <main className="dash"><h1>Admin access only.</h1><p>Assign your user the <code>admin</code> role once from Supabase after initial setup.</p></main>;return <main className="dash"><p className="eyebrow">ADMIN DASHBOARD</p><h1>Listing verification</h1><p className="subtle">Review each tutoring profile before it appears in student search.</p>{message&&<p className="notice">{message}</p>}<div className="status-list">{pending.length?pending.map(x=><article key={x.id}><b>{x.title}</b><small>{x.category} · {x.locality ? `${x.locality}, `:''}{x.city}<br />{x.description}</small><div><button className="approve" onClick={()=>review(x.id,'active')}>Approve</button><button className="reject" onClick={()=>review(x.id,'rejected')}>Reject</button></div></article>):<p>No pending listings. You’re all caught up.</p>}</div></main>}
